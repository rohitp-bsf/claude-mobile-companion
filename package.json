{
  "name": "claude-mobile-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Interact with Claude Code CLI from your mobile",
  "workspaces": [
    "server",
    "mobile",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:mobile\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:mobile": "npm run dev --workspace=mobile",
    "build": "npm run build --workspace=shared && npm run build --workspace=server && npm run build --workspace=mobile",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
